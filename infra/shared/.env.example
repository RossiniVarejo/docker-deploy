# ─── Cross-cutting / shared by all apps ──────────────────────────────────────
TZ=America/Sao_Paulo
RESTART=unless-stopped
SHARED_NETWORK_NAME=rossini-shared
NGINX_VERSION=1.27-alpine

# ─── Shared Postgres cluster ──────────────────────────────────────────────────
POSTGRES_VERSION=16-alpine
# Main admin role (used by the postgres image for init; each app gets its own role)
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASS=change-this-strong-password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DATA_PATH=/srv/docker-data/postgres
POSTGRES_BACKUP_PATH=/srv/docker-data/postgres/backups
